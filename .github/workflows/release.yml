name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-latest
            archive: linux-amd64
          - target: aarch64-unknown-linux-gnu
            runner: ubuntu-24.04-arm
            archive: linux-arm64
          - target: aarch64-apple-darwin
            runner: macos-latest
            archive: darwin-arm64
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package
        env:
          TAG_NAME: ${{ github.ref_name }}
          TARGET: ${{ matrix.target }}
          ARCHIVE: ${{ matrix.archive }}
        run: |
          cd "target/${TARGET}/release"
          tar czf "../../../skilldo-${TAG_NAME}-${ARCHIVE}.tar.gz" skilldo
          cd ../../..

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: skilldo-${{ matrix.archive }}
          path: skilldo-${{ github.ref_name }}-${{ matrix.archive }}.tar.gz

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd artifacts
          sha256sum skilldo-*.tar.gz > SHA256SUMS
          cat SHA256SUMS

      - name: Generate release notes
        id: changelog
        uses: orhun/git-cliff-action@v4
        with:
          config: cliff.toml
          args: --latest --strip header
        env:
          OUTPUT: RELEASE_NOTES.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          body_path: RELEASE_NOTES.md
          files: |
            artifacts/skilldo-*.tar.gz
            artifacts/SHA256SUMS

  homebrew:
    name: Update Homebrew tap
    needs: release
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate formula
        env:
          TAG_NAME: ${{ github.ref_name }}
        run: |
          VERSION="${TAG_NAME#v}"
          SHA_LINUX_AMD64=$(sha256sum artifacts/skilldo-${TAG_NAME}-linux-amd64.tar.gz | cut -d' ' -f1)
          SHA_LINUX_ARM64=$(sha256sum artifacts/skilldo-${TAG_NAME}-linux-arm64.tar.gz | cut -d' ' -f1)
          SHA_MACOS_ARM64=$(sha256sum artifacts/skilldo-${TAG_NAME}-darwin-arm64.tar.gz | cut -d' ' -f1)

          cat > skilldo.rb <<FORMULA
          class Skilldo < Formula
            desc "Generate SKILL.md rules files from library source code"
            homepage "https://github.com/SkillDoAI/skilldo"
            version "${VERSION}"
            license "AGPL-3.0-or-later"

            on_macos do
              on_arm do
                url "https://github.com/SkillDoAI/skilldo/releases/download/${TAG_NAME}/skilldo-${TAG_NAME}-darwin-arm64.tar.gz"
                sha256 "${SHA_MACOS_ARM64}"
              end
            end

            on_linux do
              on_intel do
                url "https://github.com/SkillDoAI/skilldo/releases/download/${TAG_NAME}/skilldo-${TAG_NAME}-linux-amd64.tar.gz"
                sha256 "${SHA_LINUX_AMD64}"
              end
              on_arm do
                url "https://github.com/SkillDoAI/skilldo/releases/download/${TAG_NAME}/skilldo-${TAG_NAME}-linux-arm64.tar.gz"
                sha256 "${SHA_LINUX_ARM64}"
              end
            end

            def install
              bin.install "skilldo"
            end

            test do
              assert_match "skilldo", shell_output("#{bin}/skilldo --help")
            end
          end
          FORMULA

          # Fix indentation (heredoc adds leading spaces)
          sed -i 's/^          //' skilldo.rb
          cat skilldo.rb

      - name: Push to homebrew-tap
        env:
          TAP_TOKEN: ${{ secrets.SKILLDO_RELEASE }}
        run: |
          git clone "https://x-access-token:${TAP_TOKEN}@github.com/SkillDoAI/homebrew-tap.git" tap
          mkdir -p tap/Formula
          cp skilldo.rb tap/Formula/skilldo.rb
          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/skilldo.rb
          git commit -m "Update skilldo to ${TAG_NAME}"
          git push
